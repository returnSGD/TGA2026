flowchart TD
    subgraph "玩家交互与游戏世界"
        A[玩家动作/环境事件] --> B{交互类型}
        B -->|抚摸/喂食/玩耍等| C[个体控制流]
        B -->|关键节点/主线推进| D[叙事控制流]
    end

    subgraph "猫咪个体控制（每只猫独立）"
        C --> E[感知与状态更新]
        E --> F[记忆模块<br/>固定窗口 + 优先级队列]
        F --> G[性格参数过滤器<br/>（黏人/胆怯/贪吃等）]
        G --> H[意图决策模块<br/>MDP + Embedding + Transformer]
        H --> I{决策输出}
        I --> J[动作意图枚举<br/>（蹭腿/叫唤/躲藏等）]
        I --> K[情感/话题标签]
        J --> L[动作控制<br/>动画状态机]
        K --> M[轻量LLM<br/>输出心理活动文本]
        M --> N[猫语文字泡]
        L --> O[猫咪骨骼动画/表情]
    end

    subgraph "整体叙事控制（全局+猫咪故事线）"
        D --> P[故事图谱<br/>（预定义节点/分支/条件）]
        P --> Q[叙事状态追踪器<br/>（进度/信任值/flag/情感基调）]
        Q --> R[LLM叙事生成<br/>受控创意]
        R --> S[一致性过滤器<br/>（事实/人设/氛围检查）]
        S --> T[生成剧情对话/场景描述]
        T --> U[更新全局叙事状态]
        U --> Q
    end

    subgraph "反馈与循环"
        O --> V[玩家看到猫咪反应]
        N --> V
        V --> W[玩家情绪/后续动作]
        W --> A
        S -.->|触发重大事件| P
        H -.->|影响信任值/解锁flag| Q
    end

    subgraph "辅助模块"
        X[设计师配置<br/>故事图/人设参数] --> P
        X --> G
        Y[离线一致性测试] -.-> S
    end
